/**
 * Just keeping track of some miscellaneous nice-to-knows!
 */

//  wireframe!!
// glPolygonMode(GL_FRONT_AND_BACK, GL_LINE);

//  setting uniforms
// int location = glGetUniformLocation(shaderProgram, "u_color");
// glUniform4f(location, 0.0f, 0.2f, 0.3f, 1.0f);